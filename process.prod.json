{
    "apps": [{
        "name": "SIP-USER",
        "script": "./bin/www",
        "instances": "max",
        "exec_mode": "cluster",
        "env": {
            "NODE_ENV": "production"
        }
    }]
}
